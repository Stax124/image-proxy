services:
  image-proxy:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    ports:
      - "8000:8000"
    volumes:
      - ./data/images:/app/data/images
    env_file:
      - .env